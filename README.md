# MS-DPMN: Multi-Scale Dynamic Pattern Matching Neural Network for Traffic Prediction

## 📌 项目简介
本仓库实现了论文 **"Multi-Scale Pattern Matching Neural Network for Traffic Prediction"** 中提出的 **MS-DPMN 模型**。  

交通流预测是智能交通系统（ITS）的核心任务，然而现有端到端模型在输入长度受限时，难以有效捕获时间与空间的趋势性和一致性。为此，我们提出 **多尺度动态记忆网络（MS-DPMN）**，通过 **动态模式匹配（DPMM）** 与 **动态掩码机制（DMM）**，在多时间尺度下学习时空异质性模式，从而提升预测性能与泛化能力。  

主要贡献：
- ✅ 提出 **多尺度动态模式匹配网络**，提升模型在长短期模式建模上的能力  
- ✅ 设计 **动态掩码模块（DMM）**，自适应选择关键时空依赖关系  
- ✅ 在 **PEMS03、PEMS04、PEMS07、PEMS08** 等真实交通数据集上显著优于现有方法  

---

## ⚙️ 环境依赖
- Python >= 3.8  
- PyTorch >= 1.13  
- CUDA >= 11.3 （建议使用 GPU 加速）  
